1.특강을 들으면서 인상적인 용어나 단어, 문장 등을 세 가지 이상 쓰세요.
(1)리눅스는 서로 다른 국적과 인종이 다른13000명이 넘는 인원이 26년간 2천만줄로 만든 오픈소스다
(2)수많은 소프트웨어들이 git 이라는 서버 관리 프로그램에 의해 만들어지고 있다.
(3)git 은 버젼관리하는 것이고 drop box 같은 것이다. 드롭박스 가면 git 처럼 예전 파일을 불러올 수 있돠.
(4)수천 수만 줄로 이루어진 코드에서 Git 을 사용하면 뭐가 바뀌었는지 쉽게 알 수 있다
(5)crud
I = input
O = ouput

git 의 Create (일종의 input) = commit
git 의 읽기 Read (일종의 output) = show log
Update
Delete
(6)코드도 결국 문서다. 엑셀 그림판 메모등등 전부 Git 으로 commit 도 하고 switch, show log 로 저장하고 바꾸고 바뀐 점을 볼 수 있다
(7)git 만 드롭박스로 올리고 다른 컴퓨터에서 깃을 깔고 그 파일을 받으면 모든 자료를 받을 수 있다
(8)개발자가 코드를 Github 에 올리면 수많은 개발자한테 보내질 수 있다.
Git 의 작업 효과 :
 git 으로 작업 로그(역사)를 볼 수 있고 예전 작업으로 switch 할 수 있는 것은 중요하다 
(9)Git 은 다른 컴퓨터로 밀(push)수 있는데 각각의 백업서버가 많을 시 이름을 정하는데 기본 이름은 origin 이다
(10)내 지역저장소(kor)에서 원격저장소(멀수록 좋은 다른곳 예를 들어 Us)로 자기 작업(Git) 을 보낼 수 있다
(11)지역저장소에서 여러개의 원격저장소를 선택할 수 있다.
(12)github에 자기가 하던 작업을 commit - push 로 해놓고 자기 사무실이나 어느 곳에서든 git 을 다운(pull)받아 작업을 이어서 하고 다시 push 로 github 에 업로드하고 집에서 볼 수있다
(13)드롭박스에서는 파일 수정할 수 있는데 다만 commit은 못하므로 git을 쓴다
(14)A라는 사람이 작업하고 push하고 B가 pull 해서 작업하고 push 하면 서로서로 마지막으로 작업된게 공유된다 즉 협업 개꿀..
(15)프로젝트 메니지 하는 사람은 git 을 잘하는 사람이다, 남들보다 쉽게 앞설수있다
(16)전산에서 생기는 동시성의 문제(2명 이상이 동시에 작업할 때)가 있다 그러나 git 은 해결할 수 있다
 >> > > > 새로온 사람이 아무것도 없을때 원격저장소를 복제하면 된다. //git clone 기능 > 원격저장소 주소를 복사붙이기 하면 새로온 사람에게도 저장된 작업들이 넘어간다
(17)전세계의 엄청나게 많은 오픈소스들이 원격저장소 주소만 있다면 git clone 으로 자기가 원하는대로 마음껏 수정할 수 있다.
(18)A와 B가 서로 동시에 작업해서 만약 A가 먼저 push 하고 B가 뒤늦게 push 하면 B가 다시 pull 해서 A 파일을 받고 push 해야된다 이때 show log 를 보면 B작업과 A작업 그리고 둘이 합쳐진게 생겨있다
(19)만약 A와 B가 한 파일에서 A는 위쪽, B는 아래쪽을 수정했을 때 A가 push 했을때 B에서 pull하면 자동으로 A가수정한거와 B가 수정한게 합쳐진다
A수정내용
원래내용
B수정내용

(20)A와 B가 동시에 같은 부분을 수정했을 시 A가 push 한 후 B 에서 pull 하면 Git 에서 충돌이 일어나면서 Git이 그 동일하게 수정한 부분만 고쳐달라고 직접 파일이 바뀌어있는게 볼 수 있다 직접 그부분을 수정해도되고 git commit master~ 에 눌러서 해결할 수 있다
> 해결된 버젼으로 바뀐다
1   >	1   >   1		 > 1
2   >	2   >   2		 > 2
A3  >	B3  >   오류(바꿔주세요)  > AB3

2.오늘 특강 중 가장 기억에 남는 단어 또는 문장을 한 줄 이상 쓰세요.



3.오늘 특강을 듣고 느낀 점을 두 줄 이상 쓰세요.