#include <stdio.h>

void ex_02() {
	/*
	문제 2
		문자열 1개를 저장하는 배열을 선언하고 사용자로부터 문자열를 입력 받아 저장하고 출력하시오. 아래 참고.
		문자열입력 : cgvs
		입력된 문자열=cgvs
	*/
	char str[20];

	printf("문자열입력 : ");
	gets(str);
	printf("입력된 문자열 = %s", str);
}

void main(void)
{
	ex_02();
	getchar();
}