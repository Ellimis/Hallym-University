#include <stdio.h>

int mystrcmp(char *p, char *q) {
	int cnt[20];

	while (*p) {
		if (*p != *q) {
			return 0;
			break;
		}
		p++;
		q++;
	}
	if (*p != *q) return 0;
	else return 1;
	
}

void ex_02() {
	/*
	strcmp(str1, str2) //문자열이 같은 지 판단
	*/
	char str1[20], str2[20];
	int cmp_n = 0;

	printf("\n비교할 문자열 중 첫번째 문자열 입력 : ");
	gets(str1);

	printf("\n비교할 문자열 중 두번째 문자열 입력 : ");
	gets(str2);

	cmp_n = mystrcmp(str1, str2);

	if (!(cmp_n)) printf("\n두 문자열 %s와 %s는 다릅니다.", str1, str2);
	else printf("\n두 문자열 %s와 %s는 같습니다.", str1, str2);
}

void main(void) {
	ex_02();
	getchar();
}