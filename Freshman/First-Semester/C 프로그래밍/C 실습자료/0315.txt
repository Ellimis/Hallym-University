#include <stdio.h>

void ex_01() {
//1.	정수인 천만 이하의 한 수를 입력 받아 우리가 사용하는 단위인 만, 천, 백, 십, 일 단위로 출력하는 프로그램을 작성하시오.
	printf("여기서 부터 1\n");
}

void ex_02(void) {
//2.	지불할 금액을 정수로 입력 받아 화폐단위가 각각 몇 개씩 필요한지 출력하는 프로그램을 작성하시오.
	int money, a, b, c, d;
	scanf_s("%lu", &money);

	a = money / 50000;
	b = money % 50000 / 10000;
	c = money % 10000 / 5000;
	d = money % 5000 / 1000;
	printf("50000원권 %d개, 10000원권 %d개, 5000원권 %d개, 1000원권 %d개 \n", a, b, c, d);

	getchar();

}

void ex_03() {
	//3.	표준입력으로 두 정수를 입력 받아 큰 수를 작은 수로 나눈 몫과 나머지를 각각 출력하는 프로그램을 작성하시오. (조건 연산자 사용)
	int a, b, max, min;
	printf("정수 입력:\n");
	scanf_s("%d", &a);
	printf("정수 입력:\n");
	scanf_s("%d", &b);
	max = (a > b) ? a : b;
	min = (a > b) ? b : a;
	printf("몫 : %d, 나머지 : %d\n", (max / min), (max%min));
	getchar();
}

void ex_04() {
	//4.	세 수를 입력 받아 가장 큰 수를 구하는 프로그램을 조건 연산자를 이용하여 작성하시오.
	int a, b, c;
	printf("세 수를 입력하세요.\n");
	scanf_s("%d%d%d", &a, &b, &c);
	(a >= b) ?
		(a >= c) ? printf("a가 가장 크다.") : printf("c가 가장 크다.")
		:
		(b >= c) ? printf("b가 가장 크다.") : printf("c가 가장 크다.");
	getchar();
}

void ex_05() {
	//5.	입력 받은 정수를 2로 나눈 몫이 10보다 크면 “OK” 그렇지 않으면 “NOK”를 출력하는 프로그램을 작성하시오.(단 나누기 연산자 사용 금지)
	int ina;
	printf("정수를 입력하세요.\n");
	scanf_s("%d", &ina);
	((ina >> 1) > 10) ? printf("OK") : printf("NOK");
	getchar();
}

void ex_06() {
	//6.	getche()를 사용하여 문자를 입력 받아 알파벳이 아니면 ‘N’, 알파벳이면 ‘Y’를 출력하는 프로그램을 작성하시오.
	char ina;
	printf("문자를 입력하세요.\n");
	ina = getche();
	
	((ina >= 'a' && ina <= 'z') || (ina >= 'A' && ina <= 'Z')) ? printf("-> Y\n") : printf("-> N\n");
	
	또는
	char ina, rst;
	rst = ((ina >= 'a' && ina <= 'z') || (ina >= 'A' && ina <= 'Z')) ? 'Y' : 
N'
	printf("-> %c\n", rst);
}

void ex_07() {
	//7.	getche()를 사용하여 숫자 2개를 입력 받아 두 숫자의 합을 출력하시오. (단, 입력 받은 문자가 숫자가 아닌 경우에는 0으로 처리하시오
	int ina, inb;
	printf("숫자 입력 : ");
	ina = getche();
	printf("숫자 입력 : ");
	inb = getche();
	printf("\n sum = %d \n", (ina-'0' + inb-'0')); // -'0' 은 입력받은 '문자(숫자)'-'문자0' 을 빼서 숫자로 출력하게 한다
	getchar();
}

void ex_08() {
	//8.	입력 받은 정수의 2배, 4배, 8배 수를 출력하는 프로그램을 작성하시오.
	int h;
	printf("정수입력 : \n");
	scanf_s("%d", &h);
	printf("2배 : %d, 4배 : %d, 8배 : %d", h<<1, h<<2, h<<3);
	getchar();
}

void main(void) {
	ex_01();
	ex_02();
	ex_03();
	ex_04();
	ex_05();
	ex_06();
	ex_07();
	ex_08();
	getchar();
}

