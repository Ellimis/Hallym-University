?lm






c1 = c(1:10); c1
c2 = c(11:20); c2
var(c1)
var(c2)
cov(c1, c2)
cor(c1, c2)


v1 = c(1:10); v1
as.data.frame(rbind(v1))
# cor = cov / sd(x)xsd(y)

z = c(1:3, NA)
is.na(z)

z<-c(1:3, NA)
is.na(z)

z = c(1:3, NA)
z==NA

c(1, 1, 1, 2) ==2

"+"(2,3)

y = c(1, 2, 3, NA)
y*3

x<-c(1:5); x
y<-seq(10, 50, 10); y
xy = rbind(x, y); xy

s<-c("Monday", "Thuesday", "Wednesday")
substr(s, 1, 2)

x<-c(1,2,3, NA); x
mean(x)

x<-1:100; x
sum(x>50)

length("statistics")
