이번 주는 Java 학습의 마지막 시간입니다.
지금까지 배운 것을 이용하여 프로그램을 만들어 봅시다.

학생은 속성으로
    id (주민번호, 그냥 정수로 취급)
    name (String)
    score (해당 교과목 점수, 정수)
    advisor (지도교수, Professor class)
를 가진다.

또한
    생성자는 (id, name, score)를 넘겨받아 초기화하며,
    getId, 
    getName, 
    getScore, 
    setAdvisor, 
    getAdvisor, 
    changeScore (매개변수로 넘어 온 점수로 점수 바꿈),
    그리고 toString (학생의 (id, 이름, score)를 하나의 문자열로 반환)
method를 가진다. 

힉생 class를 만드시오.


Professor class는
    id,
    name,
    age
속성을 가진다.

그리고
    생성자 ((id, name, age)를 넘겨받아 초기화)와
    getId, 
    getName, 
    getScore, 
    그리고 toString (교수의 (id, 이름, age)를 하나의 문자열로 반환)
method를 가진다. 

Professor class를 만드시오.


Professor는 여러 명이므로 이를 위한 ProfessorList class 가 필요하다.

ProfessorList는 여러 명이므로 이를 위하여 배열을 사용하도록 합니다.
또한 Professor의 숫자를 number라는 속성이 가지도록 합니다.

메소드로는
    생성자 2개 (매개변수가 없는 경우는 최대 20명의 교수를 가질 수 있도록 하며 정수형 매개변수 하나가 있는 경우 그 숫자만큼이 최대 교수 수)
    처음 ProfessorList가 처음 만들어지면 아직 아무 교수도 가지고 있지 않음.
    addProfessor (교수를 매개변수로 받아 ProfessorList에 추가 함)
    getById (id를 매개변수로 받아 대응되는 교수 반환)
    getByName (이름을 매개변수로 받아 대응되는 교수 반환)
    professorAt (정수를 매개변수로 받아 그 인덱스에 있는 교수 반환),
가 필요하다.

ProfessorList를 만드시오.
    
학생 또한 여러 명이므로 StudentList class 또한 필요하다.
이 또한 배열 사용

메소드로는
    생성자 2개 (ProfessorList와 유사)
    addStudent,
    StudentAt,
    getById,
    getByName,
    sortByScore (성적에 따라 descending order로),
    sortById (성적에 따라 ascending order로),
    reverse (현재 순서를 역으로)
    average (전체 학생의 평균 구하여 반환)
가 필요하다.

StudentList class를 만드시오.

다음을 참고하시오.

    public void sortById() {
        int i, j, min;
        for (i = 0; i < number-1; i++) {
            min = i;
            for (j = i+1; j < number; j++) {
                if(sts[j].getId() < sts[min].getId()) min = j;
            }
            swap(i, min);
        }
    }

그리고 마지막으로 main method를 만들어서 다양한 예를 만들어보세요.
먼저 ProfessorList를 만들고 Professor들을 만들어 ProfessorList에 추가하고
StudentList를 만들고 Student 들을 만들어 추가하고 (이 때 이미 존재하는 ProfessorList에서 id 또는 name으로 Professor를 찾아 지도교수로 만들어주고)
성적에 따라 정렬해보고, 전체 학생 List를 출력해보고
3등한 학생의 지도교수의 나이도 알아보고.....

다양한 일을 해 보세요.

    

    